[package]
name = "benchmarks"
version = "0.1.0"
edition = "2021"

[dependencies]

[dev-dependencies]
criterion = { workspace = true }
soroban-sdk = { version = "22.0.0", features = ["testutils"] }
invoice = { path = "../contracts/invoice" }
pool = { path = "../contracts/pool" }
share = { path = "../contracts/share" }
credit_score = { path = "../contracts/credit_score" }
governance = { path = "../contracts/governance" }
auction = { path = "../contracts/auction" }
referral = { path = "../contracts/referral" }

[[bench]]
name = "contract_benchmarks"
harness = false
