{
  "name": "astera-credit-score-backtest",
  "version": "1.0.0",
  "description": "Backtesting harness for the credit_score contract's scoring formula (#1041) — replays indexed payment/default events and reports whether score correlated with actual repayment outcomes.",
  "main": "src/cli.ts",
  "scripts": {
    "build": "tsc",
    "backtest": "ts-node src/cli.ts",
    "test": "ts-node test/backtest.test.ts"
  },
  "dependencies": {
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "@types/pg": "^8.10.9",
    "@types/node": "^20.10.0",
    "typescript": "^5.3.3",
    "ts-node": "^10.9.2"
  }
}
